# t22 units ledger

Units: 11 total; covered 11/11
MECH: 9
HUMAN: 1
MOOT: 1

| Unit | Class | Source unit (anonymized) | Mapping / disposition |
| --- | --- | --- | --- |
| U1 | MECH | A generalized proactive log-pruning tool (`lg`) is included. | `a01` (T2/T3: `bin/lg` and its bundled test are executable, `bash -n bin/lg` succeeds, and the full `tests/test_lg.sh` functional suite passes). The syntax probe and suite each receive a separate fresh isolated `HOME`/`TMPDIR`, cleaned immediately afterward. |
| U2 | MECH | A generalized reactive scratch-persistence tool is included. | `a02` (T2/T3/T6: `hooks/scratch-persist.py` and its bundled test are executable, the Python AST contains `extract_output`, `resolve_scratch_dir`, and `main`, and the full `tests/test_scratch_persist.sh` functional suite passes). The structural probe and suite each receive a separate fresh isolated `HOME`/`TMPDIR`, cleaned immediately afterward. |
| U3 | MECH | A generalized three-layer delegation-brief validator is included. | `a03` (T2/T3/T6: `hooks/validate-subagent-brief.py` and its bundled test are executable, the Python AST contains `required_sections_from_env`, `render_skeleton`, and `main`, and the full `tests/test_brief_validator.sh` functional suite passes). The structural probe and suite each receive a separate fresh isolated `HOME`/`TMPDIR`, cleaned immediately afterward. |
| U4 | MECH | The generalized reusable safety-hook set is included. | `a04` (T1/T2/T3/T6: all three named hooks plus `tests/test_safety_hooks.sh` are executable; the Python hook AST contains `find_risk` and `main`; the two JavaScript hooks contain their key `main`/pattern structures; and the full bundled safety-hook suite passes). Every structural probe and the suite receive separate fresh isolated `HOME`/`TMPDIR` roots, each cleaned immediately afterward. |
| U5 | MECH | Generalized multi-layer memory `recall` is included and its relationship to shared memory is documented. | `a05` (T2/T3: `bin/recall` and `tests/test_recall.sh` are executable, and a deterministic temporary script built from the bundled suite runs 19 cases plus `finish`). The only excluded invocation is `case_colon_path_survives_rg_and_grep_fallback`: its implementation unconditionally evaluates `rg_path=$(command -v rg)` under `set -e`, but host `rg` is outside the sealed standard toolchain. `case_rg_preferred_and_grep_fallback` remains included and supplies its own shims to exercise both selection branches; no failure among the other 19 cases is filtered. The initial complete-suite attempt failed solely because host `rg` was absent: every emitted case passed before the shell terminated at that unconditional lookup. `a06` checks T1 content-presence in `docs/recall.md`, using the small alternative stems `individual`/`single-agent`, `shared`/`multi-agent`, and `local-only`/`local only`. `a05` creates and executes the targeted script under a fresh isolated `HOME`/`TMPDIR`; the temporary script and environment are cleaned afterward. |
| U6 | MECH (weakened) | Personal-environment dependencies are removed or made configurable. | `a07` is a T4 sweep over tracked repository source text for literal personal absolute paths, the source username, and private workspace/repository identifiers. Validator-injected `.ev005-*` gate/fixture files are excluded because they are bundle scaffolding, not candidate repository source. Lost: semantic judgment about every possible model-specific assumption and whether every configuration boundary is ideally designed. The fixed-string sweep intentionally does not match the bracket-escaped self-test guard in `tests/test_recall.sh` (for example `[s]hojikumaru` and `/[U]sers`), so the repository's own forbidden-pattern test does not become a false positive. |
| U7 | MECH | Setup documentation and settings hook wiring reproduce installation from a clean environment. | `a08` (T1/T6: README names the settings example, placeholder replacement, and local test loop; `examples/settings.json` parses after replacing its checkout placeholder and contains nonempty pre/post-tool hook lists). The structural Python invocation runs under a fresh isolated `HOME`/`TMPDIR` and is cleaned afterward. This checks the deterministic offline core, not a live installation in a third-party application. |
| U8 | MECH (weakened) | README creation passes the three named readme-craft process gates and follows the README standard. | `a09` requires the four language README files and their shared seven-part user-facing structure (`pain`, `what`, `requirements`, `install`, `safety`, `docs`, `license`). Lost: evidence that the human process gates were performed and aesthetic/editorial inspection of the result. |
| U9 | MECH (weakened) | The eleven-item publication gate passes, including four README languages, hero/thumbnail, internal cleanup, MIT, and three-axis labels. | Replica-visible core: `a07` (T4 internal-information cleanup), `a09` (T2/T1 four structured README files), `a10` (T2 nonempty `assets/readme/hero.png`, the local hero/thumbnail asset named in the task), and `a11` (T2/T1 MIT license). Lost: the unenumerated remainder of the eleven-item gate, visual quality of the image, and live hosting-platform issue-label state, all unavailable to deterministic offline inspection. |
| U10 | HUMAN | The repository owner performs the public visibility flip. | Dropped (HUMAN: requires a named owner's approval and live external account action; it has no deterministic local behavioral core). |
| U11 | MOOT | Until the owner flip, the live repository remains private. | Dropped (MOOT: live hosting visibility is absent from the offline history-zero replica). |

## Pair span, anonymization, and needle record

- Pair: pinned pre-fix `286c2a34ecfde934417f937f324272c7da154ca2`; fix `90d5cfec89dd3107327418fb7030a4175358e27e`. The fix is an ancestor of `origin/main`.
- This is deliberately a two-milestone span: `7cc989b815c65fd08d70d42a615efcaeada6ea68` adds generalized `recall`, then `90d5cfe` adds the four-language README/documentation and hero. The pinned pre is the fix's grandparent, not `fix^`; no SHA was substituted.
- r4-3 span declaration: `a01`, `a02`, `a03`, `a04`, `a07`, and `a11` are expected to be constant-true on both the pinned pre and fix legs. The discriminating assertions expected to fail on pre are `a05`, `a06`, `a08`, `a09`, and `a10`; this 6/11 constant-true share is intentional for the two-milestone span and is confirmed by the actual pre/fix trees.
- Central spot check before authoring: `bin/recall`, three translated README files, and `assets/readme/hero.png` are absent at the pinned pre and present at the fix.
- New stand-in mapping: source repository `caty-ai/context-kit` → “workspace-toolkit repository.” Public tool names and in-repository paths remain because they are functional targets; the source repo identity is not used in task prose.
- T4 guard detail: `tests/test_recall.sh` intentionally stores its own forbidden tokens in bracket-escaped form. `a07` searches literal fixed strings, so `[s]hojikumaru`, `[a]lpha-wiki`, `[c]laude-workspace`, and `/[U]sers` do not false-positive. Validator-injected tracked paths beginning `.ev005-` are skipped so the visible gate does not inspect its own literal pattern table.
- Longest T1/T4 needle: `examples/settings.json` (22 characters, a source-named setup target). No T1/T4 assertion pins historical fix prose.
- Timeout remains the default 120 seconds. The gate runs four complete focused tool suites plus the recorded 19-case targeted recall suite, not the repository's broader aggregate test target; local validation keeps each donecheck run within this bound.
