# EV-005 acceptance-driven revision report REV2

## Summary

| Task | Finding and rule | Revision | Validity |
| --- | --- | --- | --- |
| t15 | N2; r3-1 disjunction preservation and r3-2 path derivability | Resolves one documented fixture path or fixture flag, then exercises all three validators without pinning the historical fixture layout. | [PASS](../tools/validate-logs/t15.log): pre-fix FAIL 5/5; fix PASS 5/5 |
| t11 | N3; r3-1 disjunction preservation | Accepts one shared production source or a two-file generator/generated shape with an explicit mechanical source marker. | [PASS](../tools/validate-logs/t11.log): pre-fix FAIL 5/5; fix PASS 5/5 |
| t16 | N4; r3-3 probe isolation and r3-5 standard footer | Runs every behavior probe and the targeted recall module with fresh `HOME` and `TMPDIR`; the full module remains unfiltered. | [PASS](../tools/validate-logs/t16.log): pre-fix FAIL 5/5; fix PASS 5/5 |

All three completed logs contain five numbered pre-fix runs, five numbered
fix runs, a final `VERDICT <task> PASS`, and no `DIRTY-TREE` record.

## t15 — documented path-or-flag resolution (N2; r3-1/r3-2)

- The gate scans only root README variants and `docs/repository-map.md` for
  the U4 one-line pointer. Exactly one matching line and exactly one
  backticked fixture-directory token or fixture flag are required; missing or
  ambiguous resolution fails closed.
- A relative leaf path is derived from its enclosing Markdown directory
  section. The historical fix therefore resolves `fixtures/` under the
  documented `manifests/` section without embedding `manifests/fixtures/` in
  the task.
- The path branch searches beneath the resolved directory and uses only
  pre-fix-derivable validator contracts: `--manifest`, `--manifest-dir DIR
  --all`, and `--jobs-manifest`. It accepts when each validator has a bundled
  candidate that exits 0. No fixture filename, nested README, or historical
  layout is pinned.
- The flag branch passes the exact resolved flag to each of the three named
  validators. A focused synthetic probe exercised this branch successfully;
  a duplicate-pointer probe failed closed as intended.
- Every validator attempt receives a distinct fresh `HOME` and `TMPDIR`.
- Needle/path derivability: the longest fixed structural needle is
  `python3 scripts/injection-budget-check`, whose validator name and entry
  point are criterion-constitutive and present in the pre-fix replica. The
  fixture path or flag is always derived from U4 documentation.
- Validity: **PASS**, supported by
  `experiments/ev-005/tools/validate-logs/t15.log` (five `pre exit=1`, five
  `fix exit=0`, final `VERDICT t15 PASS`).

## t11 — shared source or generator shape (N3; r3-1)

- `a08` still accepts the historical shared-library branch: both the grammar
  and refusal anchors occur together in exactly one tracked production file.
- It also accepts exactly two anchor-carrying tracked files when one contains
  a comment with `generated from` or `generated by` and names the other by its
  exact repository-relative path. Two unmarked copies, mismatched anchor
  locations, or a third copy fail closed.
- Direct behavior parity probes remain in place, so the structural alternative
  does not replace the wrapper/checker behavior requirements. A focused
  generated-copy probe exercised the second branch successfully.
- Needle/path derivability: the longest fixed structural needle remains the
  34-character assignment grammar `([A-Za-z_][A-Za-z0-9_]*)=(.*)`, which is
  present in the pre-fix replica; the generated-source path is read from the
  marker rather than prescribed by the gate.
- Validity: **PASS**, supported by
  `experiments/ev-005/tools/validate-logs/t11.log` (five `pre exit=1`, five
  `fix exit=0`, final `VERDICT t11 PASS`).

## t16 — isolated targeted recall tests (N4; r3-3/r3-5)

- `a01`–`a04` now run in separate temporary roots with fresh `HOME` and
  `TMPDIR`; `a05` independently runs the complete
  `python3 scripts/tests/test_recall.py` module with fresh values as well.
- The historical module contains a test that clears `HOME` and then expands
  `~`. A temporary Python startup hook makes only the current user's passwd
  home fallback equal the same isolated home, preventing the ambient account
  home from leaking back in. No test is skipped, filtered, or accepted after a
  nonzero exit.
- Needle/path derivability: the longest fixed structural needle is
  `scripts/tests/test_recall.py`, a tracked targeted module already present in
  the pre-fix replica. Mode `0600` and `recall` remain because the criterion
  names them directly.
- The standard footer now records the anonymization mapping, derivability basis,
  and default 120-second timeout rationale.
- Validity: **PASS**, supported by
  `experiments/ev-005/tools/validate-logs/t16.log` (five `pre exit=1`, five
  `fix exit=0`, final `VERDICT t16 PASS`).

## Review availability and disposition

- Requested Kimi K3/high: no model response. Kimi CLI 0.29.1 rejected the
  first incompatible prompt-mode flag combination, then could not create
  session state under the managed filesystem.
- Requested GLM 5.2/high: no model response. Both allowed attempts ended with
  DNS `ENOTFOUND`.
- Requested Grok 4.5/high fallback: no model response. The CLI could not create
  session state; an isolated-home retry had no reusable authentication.
- The external S/M quorum is therefore unavailable and must remain **HOLD**
  for any later merge or release decision. This does not change the completed
  on-disk validity verdicts above.

The native read-only review is advisory and does not count toward that quorum.
Its isolation and hidden nested-README findings were accepted and fixed. Its
multi-token/per-validator flag hypothesis was rejected because REV2 explicitly
requires each validator to run with the singular resolved flag, and that exact
branch was exercised. Alpha's final derivability inspection additionally
removed the literal `fixture` name constraint, so safe documented forms such as
`examples/smoke/` and `--smoke` are accepted. Native delta review cleared every
accepted finding and returned cumulative **GO** with high confidence.

## Self-verification

- PASS — t15 contains no historical fixture path or filename pin; both path
  and flag branches are live, and ambiguous pointer resolution fails closed.
- PASS — t11 accepts both the one-place and explicit generated-copy shapes.
- PASS — every t15/t16 probe invocation has fresh `HOME` and `TMPDIR`.
- PASS — t15/t16 standard footers contain mapping, longest-needle derivability,
  and timeout rationale.
- PASS — all three donechecks pass `bash -n` and ShellCheck; `git diff --check`
  is clean.
- PASS — the t11/t15/t16 logs postdate their final donechecks and contain
  completed PASS verdicts only.
- PASS — `git status --porcelain` contains only the REV2 allowlist plus the
  concurrent drafter's declared batch-3 `t17`–`t30`, logs, and report paths;
  no batch-3 path was touched by REV2.
- PASS — no commit, push, or index operation was performed.
