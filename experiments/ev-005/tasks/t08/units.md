# t08 faithfulness record

Units: 39 total; covered 39/39
MECH: 39
HUMAN: 0
MOOT: 0

| Unit | Class | Source unit (anonymized) | Mapping / disposition |
| --- | --- | --- | --- |
| U1 | MECH | `adapters/claude-code/INSTALL.md` uses the public product name in the stop-hook description. | `a01` and `a02` (T1 + T4 in `adapters/claude-code/INSTALL.md`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `cwd for a Caty Agent Harness` fragment. |
| U2 | MECH | `adapters/claude-code/INSTALL.md` uses the public product name in the precompact-hook description. | `a03` and `a04` (T1 + T4 in `adapters/claude-code/INSTALL.md`) |
| U3 | MECH | `adapters/claude-code/checkpoint-stop-hook.sh` uses the public product name in the banner line. | `a05` and `a06` (T1 + T4 in `adapters/claude-code/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `# Caty Agent Harness CHECKPOINT enforcement` fragment. |
| U4 | MECH | `adapters/claude-code/checkpoint-stop-hook.sh` uses the public product name in the workspace comment line. | `a07` and `a08` (T1 + T4 in `adapters/claude-code/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `turn in a Caty Agent Harness workspace` fragment. |
| U5 | MECH | `adapters/claude-code/checkpoint-stop-hook.sh` uses the public guard directory prefix. | `a09` and `a10` (T1 + T4 in `adapters/claude-code/checkpoint-stop-hook.sh`) |
| U6 | MECH | `adapters/claude-code/checkpoint-stop-hook.sh` uses the public CHECKPOINT prefix in the reminder message. | `a11` and `a12` (T1 + T4 in `adapters/claude-code/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `caty-agent-harness CHECKPOINT` prefix. |
| U7 | MECH | `adapters/claude-code/precompact-flush-hook.sh` uses the public product name in the banner line. | `a13` and `a14` (T1 + T4 in `adapters/claude-code/precompact-flush-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `# Caty Agent Harness pre-destruction memory flush` fragment. |
| U8 | MECH | `adapters/claude-code/precompact-flush-hook.sh` uses the public guard directory prefix. | `a15` and `a16` (T1 + T4 in `adapters/claude-code/precompact-flush-hook.sh`) |
| U9 | MECH | `adapters/claude-code/precompact-flush-hook.sh` uses the public product name in the extractor prompt. | `a17` and `a18` (T1 + T4 in `adapters/claude-code/precompact-flush-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `memory-flush extractor for a Caty Agent Harness workspace` fragment. |
| U10 | MECH | `adapters/codex/INSTALL.md` uses the public product name in the introductory paragraph. | `a19` and `a20` (T1 + T4 in `adapters/codex/INSTALL.md`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the line-wrap-tolerant `Caty Agent` product-name fragment. |
| U11 | MECH | `adapters/codex/INSTALL.md` uses the public product name in the stop-hook description. | `a21` and `a22` (T1 + T4 in `adapters/codex/INSTALL.md`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `cwd for a Caty Agent Harness` fragment. |
| U12 | MECH | `adapters/codex/checkpoint-stop-hook.sh` uses the public product name in the banner line. | `a23` and `a24` (T1 + T4 in `adapters/codex/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `# Caty Agent Harness CHECKPOINT enforcement` fragment. |
| U13 | MECH | `adapters/codex/checkpoint-stop-hook.sh` uses the public product name in the workspace comment line. | `a25` and `a26` (T1 + T4 in `adapters/codex/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `turn in a Caty Agent Harness workspace` fragment. |
| U14 | MECH | `adapters/codex/checkpoint-stop-hook.sh` uses the public guard directory prefix. | `a27` and `a28` (T1 + T4 in `adapters/codex/checkpoint-stop-hook.sh`) |
| U15 | MECH | `adapters/codex/checkpoint-stop-hook.sh` uses the public CHECKPOINT prefix in the reminder message. | `a29` and `a30` (T1 + T4 in `adapters/codex/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `caty-agent-harness CHECKPOINT` prefix. |
| U16 | MECH | `adapters/kimi/INSTALL.md` uses the public product name in the introductory paragraph. | `a31` and `a32` (T1 + T4 in `adapters/kimi/INSTALL.md`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `Caty Agent Harness CHECKPOINT enforcement` fragment. |
| U17 | MECH | `adapters/kimi/INSTALL.md` uses the public product name in the stop-hook description. | `a33` and `a34` (T1 + T4 in `adapters/kimi/INSTALL.md`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `cwd for a Caty Agent Harness workspace` fragment. |
| U18 | MECH | `adapters/kimi/checkpoint-stop-hook.sh` uses the public product name in the banner line. | `a35` and `a36` (T1 + T4 in `adapters/kimi/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `# Caty Agent Harness CHECKPOINT enforcement` fragment. |
| U19 | MECH | `adapters/kimi/checkpoint-stop-hook.sh` uses the public product name in the workspace comment line. | `a37` and `a38` (T1 + T4 in `adapters/kimi/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `turn in a Caty Agent Harness workspace` fragment. |
| U20 | MECH | `adapters/kimi/checkpoint-stop-hook.sh` uses the public guard directory prefix. | `a39` and `a40` (T1 + T4 in `adapters/kimi/checkpoint-stop-hook.sh`) |
| U21 | MECH | `adapters/kimi/checkpoint-stop-hook.sh` uses the public CHECKPOINT prefix in the reminder message. | `a41` and `a42` (T1 + T4 in `adapters/kimi/checkpoint-stop-hook.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `caty-agent-harness CHECKPOINT` prefix. |
| U22 | MECH | `docs/plugin-convention.md` uses the public product name. | `a43` and `a44` (T1 + T4 in `docs/plugin-convention.md`) |
| U23 | MECH | `templates/cron-wrapper.tmpl.sh` uses the public target-path example. | `a45` and `a46` (T1 + T4 in `templates/cron-wrapper.tmpl.sh`) |
| U24 | MECH | `templates/launchd.tmpl.plist` uses the public product name in the banner comment. | `a47` and `a48` (T1 + T4 in `templates/launchd.tmpl.plist`) |
| U25 | MECH | `templates/launchd.tmpl.plist` uses the public label example. | `a49` and `a50` (T1 + T4 in `templates/launchd.tmpl.plist`) |
| U26 | MECH | `templates/launchd.tmpl.plist` uses the public target-path example. | `a51` and `a52` (T1 + T4 in `templates/launchd.tmpl.plist`) |
| U27 | MECH | `templates/updater-cron.tmpl.sh` uses the public product name in the banner comment. | `a53` and `a54` (T1 + T4 in `templates/updater-cron.tmpl.sh`). Author-directed revision r2 (F2): was full-sentence fix-prose pin; relaxed to the `Caty Agent Harness updater cron wrapper` fragment. |
| U28 | MECH | `templates/updater-cron.tmpl.sh` uses the public repository-path example. | `a55` and `a56` (T1 + T4 in `templates/updater-cron.tmpl.sh`) |
| U29 | MECH | `tests/pause-contract.test.sh` expects the public CHECKPOINT prefix in the enabled-hook stderr assertion. | `a57` and `a58` (T1 + T4 in `tests/pause-contract.test.sh`) |
| U30 | MECH | `tests/pause-contract.test.sh` expects the public CHECKPOINT prefix in the Codex JSON assertion. | `a59` and `a60` (T1 + T4 in `tests/pause-contract.test.sh`) |
| U31 | MECH | `tests/pause-contract.test.sh` expects the public CHECKPOINT prefix in the multiworkspace stderr assertion. | `a61` and `a62` (T1 + T4 in `tests/pause-contract.test.sh`) |
| U32 | MECH | `templates/cron-wrapper.tmpl.sh` keeps the frozen cron-wrapper marker with the older prefix. | `a63` (T1 content-presence in `templates/cron-wrapper.tmpl.sh`) |
| U33 | MECH | `install.sh` keeps the frozen bootstrap marker with the older prefix. | `a64` (T1 content-presence in `install.sh`) |
| U34 | MECH | `adapters/openclaw/sentinel-cron.sh` keeps the frozen sentinel-notice marker with the older prefix. | `a65` (T1 content-presence in `adapters/openclaw/sentinel-cron.sh`) |
| U35 | MECH | The `fable-wrapper-conformance/v1` contract schema identifier stays verbatim in its contract and implementations. | `a66`–`a68` (T1 content-presence in the contract, attestation command, and conformance library) |
| U36 | MECH | The `FABLE_CONFORMING_PROVIDER_PATH` contract environment variable stays verbatim in its contract, adapter documentation, implementations, and contract test. | `a69`–`a74` (T1 content-presence in the named contract, adapter, implementation, and test files) |
| U37 | MECH | The `FABLE_WRAPPER_ROUTE` contract environment variable stays verbatim. | `a75` (T1 content-presence in `scripts/attest-wrapper`) |
| U38 | MECH | The `FABLE_WRAPPER_PATH` contract environment variable stays verbatim. | `a76` (T1 content-presence in `scripts/attest-wrapper`) |
| U39 | MECH | The `FABLE_ATTEST_SCRATCH_DIR` contract environment variable stays verbatim. | `a77` (T1 content-presence in `scripts/attest-wrapper`) |

The frozen identifier list is preserved as source code identifiers. This ledger
does not treat unrelated nominative model references or historical deployed-label
examples as product identifiers; the source explicitly distinguishes those cases.

## Anonymization and needle record

- Mapping: the public harness repository is rendered as “this repository.”
  Issue, commit, person, date, and publication provenance is omitted from the
  task sheet. The public name, retired-name removal target, and frozen
  compatibility identifiers remain because the criterion explicitly binds
  them.
- Longest T1/T4 needle: `You are a memory-flush extractor for a fable-loop
  workspace about to lose its context window.` (93 characters). It is a
  pre-fix-derived T4 baseline for the U9 rename, not newly prescribed prose;
  the positive assertion uses only the shorter public-name fragment.
- Timeout remains the default 120 seconds because all 77 assertions are
  bounded fixed-string checks and the gate executes no repository code.

## Negative validity probe (r5-1)

- Minimal non-solution edit: Append public-name assertion needles only as comments to the Claude/Codex/Kimi INSTALL and hook files, `docs/plugin-convention.md`, `templates/cron-wrapper.tmpl.sh`, `templates/launchd.tmpl.plist`, `templates/updater-cron.tmpl.sh`, and `tests/pause-contract.test.sh`, leaving retired live strings and behavior intact.
- Route: `a`
- Expected result: `a02`, `a04`, `a06`, `a08`, `a10`, `a12`, `a14`, `a16`, `a18`, `a20`, `a22`, `a24`, `a26`, `a28`, `a30`, `a34`, `a36`, `a38`, `a40`, `a42`, `a44`, `a46`, `a48`, `a50`, `a52`, `a54`, `a56`, `a58`, `a60`, and `a62` should still FAIL.
- Evidence status: `EXPECTED_FAIL_CONFIRMED`; failing CHECK IDs: `a02`, `a04`, `a06`, `a08`, `a10`, `a12`, `a14`, `a16`, `a18`, `a20`, `a22`, `a24`, `a26`, `a28`, `a30`, `a34`, `a36`, `a38`, `a40`, `a42`, `a44`, `a46`, `a48`, `a50`, `a52`, `a54`, `a56`, `a58`, `a60`, `a62`; `RUN t08 negprobe exit=1 dur=0s`; no `DIRTY-TREE`; log: `experiments/ev-005/tools/validate-logs/negprobe/t08.log`.
- Rationale: The non-solution keeps the retired live strings in place, so the broad T4 absence set should remain fail-closed.

## Constant-true declaration (r5-2)

- Source log: `experiments/ev-005/tools/validate-logs/t08.log` (current pre-leg record).
- a03 — oversight: A generic phrase already present in the pre tree satisfies the check without proving the targeted contract change.
- a32 — oversight: A newline-split retired phrase evades the fixed-string absence probe, so the pre leg passes vacuously.
- a63 — invariance guard: The frozen marker check already holds in the pre tree and intentionally protects it.
- a64 — invariance guard: The paired frozen marker already holds pre-fix, so the PASS is deliberate.
- a65 — invariance guard: The schema guard already holds on the pre tree and intentionally stays constant-true.
- a66 — invariance guard: The paired schema guard already holds on the pre tree.
- a67 — invariance guard: The provider environment-variable guard already holds pre-fix, protecting against regressions.
- a68 — invariance guard: The paired provider environment-variable guard already holds pre-fix.
- a69 — invariance guard: The next frozen marker/schema invariant already holds on the pre tree.
- a70 — invariance guard: The paired frozen marker/schema invariant already holds pre-fix.
- a71 — invariance guard: The next provider-env invariant already holds before the fix.
- a72 — invariance guard: The paired provider-env invariant already holds before the fix.
- a73 — invariance guard: The final frozen marker invariant already holds pre-fix and is intentionally guarded.
- a74 — invariance guard: The paired frozen marker invariant already holds pre-fix.
- a75 — invariance guard: The final schema/provider invariant already holds on the pre tree.
- a76 — invariance guard: The paired final schema/provider invariant already holds on the pre tree.
- a77 — invariance guard: The closing frozen-provider invariant already holds pre-fix and intentionally remains constant-true.
