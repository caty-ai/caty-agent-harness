# producer=declared-current-model
# reviewer glm-5.3 /absolute/path/to/reviewer --flag
# reviewer fallback-model /absolute/path/to/fallback --flag
# notify_cmd=/absolute/path/to/notifier
# review_window_weeks=2
# reviewer_timeout_s=600
# fabricated_floor=2
# Whole call fails at max(fabricated_floor, ceil(fabricated_pct% of blocks)).
# fabricated_pct=50
# zero_streak_threshold=14
# prompt_max_bytes=2000000
