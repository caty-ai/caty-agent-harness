## Verified facts      (cap 120 lines)
<!-- Entry format: one fact per bullet, absolute date (YYYY-MM-DD), provenance (source task or job id, and verifier id if verified); entries may carry invalidated-by: pointers. -->
## General rules       (cap 80)
<!-- Entry format: one fact per bullet, absolute date (YYYY-MM-DD), provenance (source task or job id, and verifier id if verified); entries may carry invalidated-by: pointers. -->
## Open failures       (cap 100)
<!-- Entry format: one fact per bullet, absolute date (YYYY-MM-DD), provenance (source task or job id, and verifier id if verified); entries may carry invalidated-by: pointers. -->
## Lessons learned     (cap 60)
<!-- Entry format: one fact per bullet, absolute date (YYYY-MM-DD), provenance (source task or job id, and verifier id if verified); entries may carry invalidated-by: pointers. -->
## Last session        (cap 20 entries — newest first; entry 1 carries the 4 fields inline)
<!-- Entry 1: - YYYY-MM-DD | task id | next: ... | blockers: ... | artifact: ... | handoff: loop/handoffs/<date>-<slug>.md -->
<!-- Entries 2..20: - YYYY-MM-DD | task id | handoff: loop/handoffs/<date>-<slug>.md -->
